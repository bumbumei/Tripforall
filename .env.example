# 한국관광공사 TourAPI 4.0 인증키 (data.go.kr 발급)
TOUR_API_KEY=

# LLM 키 — 하나만 있으면 됨. 둘 다 있으면 OpenAI 우선.
# 둘 다 없으면 결정론적 fallback 결과로 화면은 정상 동작.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# 데모 모드 (TourAPI 키 없을 때 mock 데이터 사용)
USE_MOCK_TOUR_API=true
